<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.MisLugares" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/primary</item>
        <item name="colorPrimaryVariant">@color/primary</item>
        <item name="colorOnPrimary">@color/on_primary</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/secondary</item>
        <item name="colorSecondaryVariant">@color/secondary</item>
        <item name="colorOnSecondary">@color/on_primary</item>

        <item name="android:windowBackground">@color/background</item>

        <!-- Status bar color. -->
        <item name="android:statusBarColor">@color/background</item>
        <item name="android:windowLightStatusBar">true</item>
    </style>

    <style name="Theme.MisLugares.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="Theme.MisLugares.AppBarOverlay" parent="ThemeOverlay.AppCompat.Light">
        <item name="colorControlNormal">@color/primary</item>
    </style>
    <style name="Theme.MisLugares.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="Widget.MisLugares" parent="" />

    <!-- Custom Card Style -->
    <style name="Widget.MisLugares.CardView" parent="Widget.Material3.CardView.Elevated">
        <item name="cardBackgroundColor">@color/card_background</item>
        <item name="cardCornerRadius">24dp</item>
        <item name="cardElevation">2dp</item>
        <!-- Retiramos el borde negro feo que se solapaba y daba ruido. M3 usa elevación sutil -->
    </style>

    <!-- Custom Button Style -->
    <style name="Widget.MisLugares.Button" parent="Widget.Material3.Button">
        <item name="backgroundTint">@color/primary</item>
        <item name="android:textColor">@color/on_primary</item>
        <item name="shapeAppearanceOverlay">@style/ShapeAppearance.MisLugares.Button</item>
    </style>

    <style name="ShapeAppearance.MisLugares" parent="" />

    <style name="ShapeAppearance.MisLugares.Button" parent="">
        <item name="cornerFamily">rounded</item>
        <item name="cornerSize">24dp</item>
    </style>

    <!-- Filter Chip Style -->
    <style name="Widget.MisLugares.Chip" parent="Widget.Material3.Chip.Filter">
        <item name="chipBackgroundColor">@color/chip_background_color</item>
        <item name="android:textColor">@color/chip_text_color</item>
        <item name="chipStrokeColor">@color/outline</item>
        <item name="chipStrokeWidth">1dp</item>
        <item name="shapeAppearanceOverlay">@style/ShapeAppearance.MisLugares.Chip</item>
    </style>

    <style name="ShapeAppearance.MisLugares.Chip" parent="">
        <item name="cornerFamily">rounded</item>
        <item name="cornerSize">12dp</item>
    </style>

    <!-- Category Tag Style -->
    <style name="Widget.MisLugares.CategoryTag">
        <item name="android:background">@drawable/category_tag_background</item>
        <item name="android:paddingLeft">8dp</item>
        <item name="android:paddingRight">8dp</item>
        <item name="android:paddingTop">4dp</item>
        <item name="android:paddingBottom">4dp</item>
        <item name="android:textColor">@color/black</item>
        <item name="android:textSize">@dimen/text_size_tag</item>
        <item name="android:textStyle">bold</item>
        <item name="android:textAllCaps">true</item>
    </style>
</resources>